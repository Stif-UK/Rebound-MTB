import 'package:flutter/material.dart';
import 'package:rebound_mtb/Rebound.dart';

void main() {
  runApp(
      new MaterialApp(
          title: 'Rebound',
          home: new Rebound()
      )
  );
}
