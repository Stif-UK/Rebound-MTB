import 'package:flutter/material.dart';
import 'package:rebound_mtb/ui/Rebound.dart';

void main() {
  runApp(
      new MaterialApp(
          title: 'Rebound',
          home: new Rebound()
      )
  );
}
